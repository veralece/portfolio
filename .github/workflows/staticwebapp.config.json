{
    "routes": [
        {
            "route": "/about"
        },
        {
            "route": "/resume"
        },
        {
            "route": "/projects"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": [
            "/images/*.{png,jpg,gif}",
            "/css/*"
        ]
    }
}